---
title: yarn esfuse run
---

The `yarn esfuse run` command can be seen as an analogue of `ts-node/register`, `@babel/register`, or even Deno. It will run the specified script without having to transpile it to JavaScript first.
